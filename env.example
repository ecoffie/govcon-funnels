# Copy to .env.local and fill in your values (do not commit .env.local).

# --- CRM: GoHighLevel ---
# Get API key from GHL: Settings → API Keys. Use a Location-level key for the location below.
GHL_API_KEY=
GHL_LOCATION_ID=

# --- CRM: Generic webhook (optional) ---
# Use this for Zapier, Make, n8n, or any CRM that accepts a webhook.
# Payload sent: { name, email, phone, source, redirectUrl, timestamp }
CRM_WEBHOOK_URL=

# --- Slack lead notifications ---
# Create an incoming webhook at https://api.slack.com/messaging/webhooks
SLACK_LEAD_WEBHOOK_URL=

# --- Email (Gmail SMTP) ---
# Use a Gmail account with an app password (not your regular password).
# Generate one at: Google Account → Security → 2-Step Verification → App passwords
SMTP_USER=
SMTP_PASSWORD=
